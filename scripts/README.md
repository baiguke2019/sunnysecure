# Local scripts

Put one-off forensic / probe / experiment scripts here.

**Do not commit live credentials.** This directory is gitignored except this README.
Load secrets from `../forensics/` (also gitignored) or environment variables.
