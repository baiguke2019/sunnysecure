module.exports = {
  apps: [
    {
      name: "autosecure-api",
      cwd: __dirname,
      script: ".venv/bin/python",
      args: "-m uvicorn app:app --host 127.0.0.1 --port 8000 --app-dir web",
      env: {
        // Add your public UI origin(s) for CORS
        CORS_ORIGINS:
          "http://127.0.0.1:3000,http://localhost:3000",
      },
      autorestart: true,
      max_restarts: 10,
    },
    {
      name: "autosecure-web",
      cwd: __dirname,
      script: "node",
      args: "web/.output/server/index.mjs",
      env: {
        NITRO_HOST: "0.0.0.0",
        HOST: "0.0.0.0",
        PORT: "3000",
        NITRO_PORT: "3000",
      },
      autorestart: true,
      max_restarts: 10,
    },
    {
      name: "autosecure-bot",
      cwd: __dirname,
      script: ".venv/bin/python",
      args: "bot.py",
      autorestart: true,
      max_restarts: 10,
    },
  ],
};
